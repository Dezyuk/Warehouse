<UserControl x:Class="Warehouse.Views.OutboundInvoiceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:Warehouse.ViewModels"
             x:Name="outboundView">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Кнопка для добавления товара в накладную -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,0,0,10">
            <Button Content="Добавить товар" Command="{Binding AddProductCommand}" Margin="5"/>
        </StackPanel>

        <!-- DataGrid для отображения списка позиций в накладной -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding Invoice.OrderProducts}" AutoGenerateColumns="False" Margin="5">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Товар" Binding="{Binding Product.Name}" Width="*"/>
                <DataGridTextColumn Header="Количество" Binding="{Binding Quantity}" Width="Auto"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Кнопка для сохранения накладной -->
        <Button Content="Сохранить накладную" Command="{Binding SaveInvoiceCommand}" Grid.Row="2" HorizontalAlignment="Right" Margin="5"/>
    </Grid>
</UserControl>
