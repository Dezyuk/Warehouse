<Window x:Class="Warehouse.Views.OrderProductEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Warehouse.Views"
        mc:Ignorable="d"
        Title="OrderProductEditWindow" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Продукт:" />
        <TextBlock x:Name="ProductNameTextBlock" Grid.Row="1" FontWeight="Bold" Margin="0,5,0,10"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" VerticalAlignment="Center">
            <TextBlock Text="Количество:" VerticalAlignment="Center"/>
            <TextBox x:Name="QuantityTextBox" Width="100" Margin="10,0,0,0"/>
            <Button Content="ОК" Width="60" Margin="10,0,0,0" Click="ConfirmButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
