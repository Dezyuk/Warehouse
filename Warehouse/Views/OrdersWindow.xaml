<Window x:Class="Warehouse.Views.OrdersWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Warehouse.Views"
        mc:Ignorable="d"
        Title="Управление заказами" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,0,0,10">
            <Button Content="Добавить заказ" Click="AddOrder_Click" Margin="5"/>
            <Button Content="Редактировать заказ" Click="EditOrder_Click" Margin="5"/>
            <Button Content="Удалить заказ" Click="DeleteOrder_Click" Margin="5"/>
        </StackPanel>

        <DataGrid x:Name="OrdersGrid" Grid.Row="1" AutoGenerateColumns="True" ItemsSource="{Binding Orders}"/>
    </Grid>
</Window>
